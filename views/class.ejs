<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title></title>
    <meta name="description" content="">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="/main.css">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.7.1/css/all.css" integrity="sha384-fnmOCqbTlWIlj8LyTjo7mOUStjsKC4pOpQbqyi7RrhN7udi9RwhKkMHpvLbHG9Sr"
        crossorigin="anonymous">
</head>

<body>
    <nav>
        <a href="/profile"><i class="fas fa-user"></i> Profile</a>
        <a href="/logout"><i class="fas fa-sign-out-alt"></i> Sign Out</a>
        <a href="/home"><i class="fas fa-book"></i> Home</a>
    </nav>
    <form id="popup_delete">
            <h1>EasyBook</h1>
            <h2 id="popup-header">Delete this student?</h2>
            <label for="" id="popup-label">Are you sure you want to delete this student?</label>
            <br><br>
            <input type="button" name="" value="Yes, I'm Sure" class="uibutton" id="accept">
            <input type="button" name="" value="Cancel" class="uibutton" id="cancel">
        </form>
    <section>
            <h1><%= viewClass.name %></h1>
            <h2><%= viewClass.teacher.name %></h2>
            <% if(code) { %><span id="code"><%= code %></span><% } %>
            <div class="book">
                <h1><%= viewClass.book.name %></h1>
                <h2><%= viewClass.book.author %></h2>
                <a href="<%= viewClass.book.link %>" class="uibutton">Read Book</a>
            </div>
           </section>
  <% if(type === "teacher") { %>
    <div class="teacher" cid="<%= viewClass._id %>" id="class">
            <section>
                    <h1 id="students-header">Students (<%= viewClass.students.length %>)</h1>
                    <ul>
                      <% for(var i in viewClass.students) { %>
                      <li><%= viewClass.students[i].name %> <span id="code"><%= viewClass.students[i].code %></span> <i class="fas fa-times delete-student"></i></li>
                      <% } %>
                    </ul>
                   </section>
                   <section>
                        <h1 id="codes-header">Unredeemed Codes (<%= viewClass.codes.length %>)</h1>
                        <ul>
                          <% for(var i in viewClass.codes) { %>
                          <li><span id="code"><%= viewClass.codes[i] %></span> <i class="fas fa-times delete-code"></i></li>
                          <% } %>
                        </ul>
                       </section>
    </div>
  <% } %>
    <script src="/class.js" async defer></script>
</body>

</html>